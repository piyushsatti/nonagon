# MongoDB connection string shared by both API and bot containers.
MONGO_URI=""

# Logical database name used by the API when connecting to MongoDB.
DB_NAME=""

# Discord credentials
# BOT_TOKEN is required for the bot to authenticate with the gateway.
BOT_TOKEN=""

# When set to a truthy value (1/true/yes/on), the bot will auto-load the
# full set of default cogs on startup. Leave unset or false to load only core
# admin cogs and use the extension manager to load others interactively.
AUTO_LOAD_COGS=0
